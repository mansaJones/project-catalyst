# Sample ad images

The app shows these when a sample is loaded (see src/samples.ts). Save each
exported Canva JPG here with the exact filename:

- ledgerly.jpg     — Ledgerly (B2B SaaS)
- drift.jpg        — Drift (DTC consumer)
- capitalpulse.jpg — Capital Pulse (fintech)

Missing files are hidden gracefully by the app (the <img> onError handler).
