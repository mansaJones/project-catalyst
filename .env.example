# Copy to .env and fill in. .env is gitignored — never commit your key.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides:
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# PORT=8787

# --- frontend (Vite) ---
# VITE_USE_LLM=true
# VITE_API_BASE=            # leave blank for local (same-origin); set to the hosted API URL in prod

# --- backend (production hosting) ---
# ALLOWED_ORIGIN=https://project-catalyst.mansa-tech.com
# RATE_LIMIT_PER_MIN=20
